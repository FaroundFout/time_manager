{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for app windows",
  "windows": ["main", "reminder"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open",
    "fs:allow-read-text-file",
    "notification:default"
  ]
}
