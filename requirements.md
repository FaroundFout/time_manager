# 时间管理程序需求文档

最后更新：2026-06-02

本文档是第一版开发依据。目标是让新的开发对话、开发者或后续的我，在不依赖原聊天记录的情况下，直接根据本文档理解产品规则并开始实现。

当前阶段只整理需求，不编写代码。

## 1. 产品定位

本程序是一款 Windows 本地桌面时间管理工具。

核心目标不是普通待办清单，而是根据用户提供的每日作息模板，驱动用户按阶段执行，并通过强确认机制记录真实执行情况。

程序的核心原则：

- 每个正式阶段都需要用户手动确认开始。
- 每个正式阶段都需要用户手动确认结束。
- 没有确认，就不自动进入下一阶段。
- 任意确认延迟都会影响后续时间线。
- 突发事件期间不计入作息时间。
- 历史记录保留原计划、当前计划和实际执行，用于对比。
- 第一版完全本地运行，不需要账号、云同步或联网。

## 2. 第一版目标

第一版需要实现一个可用的 Windows 本地桌面程序，具备：

- 读取用户提供的每日固定作息 Markdown 文件。
- 根据作息模板生成当前作息日时间线。
- 到点后弹出提醒，要求用户确认开始或结束。
- 用户未确认时，当前状态保持不变，后续日程按规则顺延。
- 支持突发事件按钮，进入暂停作息时间的状态。
- 支持主界面查看今日时间线。
- 支持历史页按作息日查看执行记录。
- 支持本地持久化，程序关闭、重启、睡眠唤醒后恢复状态。
- 支持 Windows 系统托盘。
- 支持系统通知、桌面提醒弹窗和声音提醒。

## 3. 非目标

第一版不做以下功能：

- 不做账号系统。
- 不做云同步。
- 不做移动端。
- 不做网页端。
- 不做工作日和周末区分。
- 不做多个作息模板。
- 不做复杂统计图表。
- 不做历史导出，但数据结构要方便以后导出 Markdown 或 CSV。
- 不做历史编辑。
- 不做补录实际开始时间或结束时间。
- 不做全局快捷键。
- 不做免打扰或静音一段时间。
- 不做自定义提醒音频文件。
- 不做时间模拟或快进模式。
- 不做自动数据库备份。
- 不做便携绿色版。

## 4. 推荐技术方案

第一版推荐技术栈：

- 桌面框架：Tauri
- 前端：React
- 语言：TypeScript
- 本地数据：SQLite
- 目标系统：Windows
- 分发形式：安装版

选择理由：

- Tauri 适合本地桌面工具，体积相对轻。
- Windows 托盘、通知、窗口管理和开机自启动都可以覆盖。
- React 和 TypeScript 适合构建状态清晰的时间线界面。
- SQLite 适合保存历史记录、事件流和当前状态。

## 5. 关键术语

### 5.1 作息模板

用户提供的 Markdown 文件，描述每天固定的时间安排。

### 5.2 作息日

作息日不是自然日，不按 00:00 强行切分。

作息日以“日程起点”为边界。例如日程起点是 08:00，则一个作息日可以理解为：

```text
当天 08:00 到次日 08:00 前
```

跨午夜阶段允许存在，例如：

```text
23:30-00:30 学习
```

它表示从当前作息日的 23:30 到次日 00:30。

### 5.3 正式阶段

用户在 Markdown 模板中显式写出的时间段，例如：

```text
10:00-11:30 学习
```

正式阶段必须确认开始，也必须确认结束。

### 5.4 自动空档

模板中两个正式阶段之间没有显式写出的间隙。

例如：

```text
09:00-10:00 学习
11:00-12:00 会议
```

10:00 到 11:00 是自动空档。

自动空档不需要确认开始或结束，但需要显示在时间线和历史记录中。

### 5.5 突发事件

用户主动按下突发事件按钮后进入的暂停状态。

突发事件期间不计入正式阶段耗时，也不计入等待确认延迟。突发事件结束后，作息时间从断点继续。

### 5.6 原计划时间

Markdown 模板中定义的时间。

原计划永远保留，用于对比，不因为延迟、提前、突发事件而改变。

### 5.7 当前计划时间

根据用户实际确认、提前结束、立即进入下一阶段、突发事件等事件推导出来的当前时间线。

当前计划可能晚于原计划，也可能早于原计划。

### 5.8 实际时间

用户真实点击确认开始、确认结束、提前结束、开始突发事件、结束突发事件等操作时记录的时间。

第一版不允许补录或编辑实际时间。

## 6. 作息 Markdown 格式

### 6.1 基本格式

第一版支持简单格式：

```markdown
日程起点: 08:00

- 08:00-08:30 起床
- 08:30-09:00 早餐
- 09:00-11:30 学习
- 11:30-13:00 午饭
- 13:00-14:30 学习
- 14:30-15:00 休息
```

### 6.2 日程起点

允许显式声明日程起点：

```markdown
日程起点: 08:00
```

如果没有显式声明，则自动使用第一项正式阶段的开始时间作为日程起点。

显式日程起点和自动推断并存，优先级如下：

1. 优先使用 Markdown 中显式声明的日程起点。
2. 没有显式声明时，使用第一项正式阶段的开始时间。

### 6.3 时间格式

第一版支持：

```text
HH:MM-HH:MM 名称
```

允许：

- 普通冒号，例如 `08:00`
- 中文冒号，例如 `08：00`
- 时间段前后有空格，例如 `08:00 - 09:00 起床`
- 阶段名称重复
- 阶段名称为中文、英文或混合文本
- 可选备注

不支持：

- 自然语言时间，例如“早上八点”
- 没有结束时间的阶段
- 日期级别的不同日程
- 工作日和周末不同模板
- 多模板

### 6.4 可选备注

第一版允许可选备注，但不做复杂结构。

示例：

```markdown
- 09:00-11:30 学习 # 备注：数学
```

备注只用于显示和历史记录，不参与时间计算。

### 6.5 阶段顺序

Markdown 中的阶段必须按作息顺序书写。

程序不自动排序。

如果阶段顺序不合理，视为解析错误。

### 6.6 重叠阶段

两个正式阶段不允许重叠。

示例错误：

```markdown
- 08:00-10:00 学习
- 09:30-11:00 会议
```

出现重叠时，不允许应用该模板。

### 6.7 跨午夜阶段

允许结束时间小于开始时间。

示例：

```markdown
- 23:30-00:30 学习
```

这表示从当前作息日 23:30 到次日 00:30。

## 7. 模板应用规则

### 7.1 首次启动

如果程序第一次启动，尚未选择作息 Markdown 文件：

- 显示初始化界面。
- 要求用户选择 `.md` 作息文件。
- 成功解析后，用户需要确认启用。
- 在成功启用前，不启动提醒。

初始化界面可以提供“查看示例格式”入口。

### 7.2 首次启用时的定位

首次启用作息文件时，根据当前时间定位到当前作息日中的对应位置。

如果当前时间落在某个正式阶段内：

- 进入该阶段的“等待开始确认”状态。
- 不自动认为用户已经开始。
- 用户必须手动确认开始。

如果当前时间落在自动空档内：

- 进入空档状态。
- 等待下一正式阶段开始。

如果当前时间早于今日作息日起点：

- 进入等待今日作息开始状态。
- 到点后提醒确认第一阶段开始。

如果当前时间晚于当前作息日最后一个阶段结束：

- 不追溯今天已经结束的日程。
- 等待下一个作息日开始。

### 7.3 模板修改

Markdown 文件修改后，不自动影响当前作息日。

用户必须手动点击“应用最新日程”，并通过二次确认后，最新模板才会应用。

### 7.4 应用最新日程

手动应用最新日程时：

- 解析最新 Markdown 文件。
- 如果解析失败，不应用。
- 如果解析成功，需要用户二次确认。
- 当前已发生历史记录保留。
- 当前正在进行或等待确认的阶段标记为“因应用新日程而终止”。
- 之后从当前时间按最新版模板重新生成当前作息日状态。

从当前时间按新模板重新生成时：

- 当前时间落在正式阶段内，则进入该阶段的等待开始确认。
- 当前时间落在自动空档内，则进入空档。
- 当前时间早于日程起点，则等待今日作息开始。
- 当前时间晚于当前作息日最后阶段，则等待下一作息日开始。

## 8. 核心状态机

正式阶段的基本流转：

```text
等待开始确认 -> 进行中 -> 等待结束确认 -> 下一个阶段等待开始确认
```

没有额外的“准备进入下一阶段”确认。

主要状态：

- 未初始化
- 等待作息日开始
- 自动空档中
- 等待阶段开始确认
- 阶段进行中
- 等待阶段结束确认
- 突发事件中
- 等待下一个作息日
- 数据错误或初始化错误

### 8.1 等待阶段开始确认

当某个正式阶段到了当前计划开始时间，程序进入等待开始确认。

此时：

- 弹出提醒。
- 周期性提醒。
- 用户确认前，不进入该阶段进行中。
- 后续所有正式阶段根据等待时长顺延。
- 如果用户关闭或最小化提醒窗口，状态仍保持等待开始确认。

确认开始时：

- 记录实际开始时间为点击确认时刻。
- 阶段进入进行中。
- 阶段持续时长保持模板定义的时长。
- 当前计划结束时间根据实际开始时间和固定时长重新计算。

### 8.2 阶段进行中

阶段进行中时：

- 显示阶段名称。
- 显示距离预计结束的倒计时。
- 显示已进行时间。
- 支持开始突发事件。
- 支持提前结束。

到当前计划结束时间后：

- 进入等待结束确认。
- 弹出提醒。
- 不自动结束。

### 8.3 等待阶段结束确认

当阶段到达当前计划结束时间后，进入等待结束确认。

此时：

- 当前阶段仍被视为进行中。
- 超出的时间计为阶段延长时间。
- 后续阶段继续顺延。
- 周期性提醒。
- 用户确认结束前，不进入下一阶段。

确认结束时：

- 记录实际结束时间为点击确认时刻。
- 当前阶段完成。
- 如果下一阶段当前计划开始时间已经到达，则立即进入下一阶段等待开始确认并提醒。
- 如果下一阶段当前计划开始时间未到，则进入自动空档或等待状态。

### 8.4 自动空档中

自动空档不需要确认。

空档期间：

- 主界面显示当前处于空档。
- 显示下一个正式阶段及其当前计划开始时间。
- 可以开始突发事件。
- 到下一正式阶段当前计划开始时间时，进入等待开始确认。

### 8.5 突发事件中

突发事件开始后：

- 记录突发事件开始时间。
- 程序进入突发事件中。
- 显示持续计时器。
- 提供结束突发事件按钮。
- 作息时间暂停。

突发事件结束后：

- 记录结束时间。
- 允许用户填写可选备注。
- 不填则名称为“突发事件”。
- 根据突发事件发生的位置恢复日程。

## 9. 时间计算规则

### 9.1 阶段时长固定

正式阶段的计划时长由模板决定。

如果开始确认延迟，阶段持续时长不变。

示例：

```text
原计划：10:00-11:30 学习
计划时长：90 分钟
实际确认开始：10:17
当前计划结束：11:47
```

### 9.2 开始确认延迟

如果到了阶段开始时间，用户没有确认开始：

- 程序停留在等待开始确认。
- 等待时长计为开始延迟。
- 后续时间线顺延。

示例：

```text
原计划开始：10:00
实际确认开始：10:17
开始延迟：17 分钟
后续阶段顺延：17 分钟
```

### 9.3 结束确认延迟

如果到了阶段结束时间，用户没有确认结束：

- 当前阶段继续进行。
- 超出时间计为阶段延长。
- 后续时间线继续顺延。

示例：

```text
当前计划结束：11:47
实际确认结束：12:05
阶段延长：18 分钟
后续阶段额外顺延：18 分钟
```

### 9.4 提前结束

阶段进行中时，用户可以提前结束。

提前结束规则：

- 需要二次确认。
- 实际结束时间为确认提前结束的时刻。
- 当前阶段被缩短。
- 默认不提前后续阶段。
- 当前阶段结束到下一阶段当前计划开始之间形成空档。
- 空档需要显示并记录到历史。

示例：

```text
原计划学习：10:00-11:30
实际开始：10:00
提前结束：11:00
下一阶段原计划开始：11:30
结果：11:00-11:30 为空档
```

### 9.5 立即进入下一阶段

提前结束后，用户可以选择立即进入下一阶段。

规则：

- 需要二次确认。
- 该操作表示用户显式允许后续时间线提前。
- 下一阶段实际开始时间为确认时刻。
- 后续阶段基于该实际开始时间重新计算。
- 原计划时间保留用于对比。
- 当前计划时间可以早于原计划时间。

### 9.6 当前计划可能提前或延后

当前计划不是简单的“原计划加一个固定延迟”。

程序应基于事件流计算当前计划：

- 开始晚了，后续偏移增加。
- 结束晚了，后续偏移增加。
- 提前结束但不立即进入下一阶段，后续偏移不变，产生空档。
- 立即进入下一阶段，后续偏移可能减少。
- 突发事件占用作息时间，后续偏移增加。
- 突发事件发生在纯空档且没有越过下一阶段开始时间，后续偏移不变。

### 9.7 突发事件发生在正式阶段进行中

示例：

```text
学习：10:00-11:30
10:40 开始突发事件
11:10 结束突发事件
```

结果：

- 10:40 到 11:10 不计入学习时间。
- 学习剩余时间继续保留。
- 学习结束时间从 11:30 顺延到 12:00。

### 9.8 突发事件发生在等待确认状态

如果正在等待开始确认或等待结束确认时进入突发事件：

- 突发事件期间不继续累计等待确认延迟。
- 突发事件结束后继续等待确认。
- 突发事件时间单独记录。

示例：

```text
10:00 等待开始学习
10:05 开始突发事件
10:35 结束突发事件
10:40 确认开始
```

开始延迟只计算：

```text
10:00-10:05
10:35-10:40
```

总计 10 分钟。

### 9.9 突发事件发生在自动空档中

如果突发事件完全落在自动空档内，且没有跨过下一阶段开始时间：

- 不顺延后续正式阶段。
- 自动空档被切成两段。
- 中间插入突发事件记录。

示例：

```text
空档：09:00-10:00
突发事件：09:20-09:40
```

历史显示：

```text
空档：09:00-09:20
突发事件：09:20-09:40
空档：09:40-10:00
```

### 9.10 突发事件跨过阶段边界

如果突发事件跨过了原本应该开始的阶段：

- 恢复后进入该阶段等待开始确认。
- 被突发事件覆盖的时间算突发事件顺延。
- 不算作用户开始延迟。

示例：

```text
空档：09:00-10:00
学习：10:00-11:30
突发事件：09:50-10:20
```

恢复后：

```text
学习原计划开始：10:00
因突发事件顺延后当前计划开始：10:20
状态：等待开始确认
```

### 9.11 作息日结束

上一作息日所有阶段完成后，程序可以进入下一作息日。

如果到了下一作息日起点但上一作息日还没完成，通常说明用户忘记确认。

处理方式：

- 不自动清空或跳过。
- 保持当前未完成状态。
- 主界面明显提示“可能忘记确认”。
- 用户可以选择继续当前状态、补做正常确认，或使用重置当前作息日。

## 10. 重置当前作息日

程序需要提供“重置当前作息日”功能。

用途：

- 忘记确认导致状态残留。
- 昨天的作息还未完成。
- 当天节奏完全打乱，需要重新开始。

规则：

- 需要二次确认。
- 保留已发生的历史记录。
- 当前正在进行或等待确认的阶段标记为“因手动重置而终止”。
- 未开始的后续阶段标记为未完成或放弃。
- 记录一次“手动重置”事件。
- 之后根据当前时间重新定位作息状态。

历史不能被直接删除。

## 11. 程序关闭、退出和恢复

### 11.1 主窗口关闭

点击主窗口关闭按钮时：

- 不退出程序。
- 默认隐藏到系统托盘。
- 后台继续运行提醒和状态计算。

### 11.2 真正退出

真正退出只能通过：

- 托盘菜单中的退出。
- 主界面中的退出按钮。

退出前需要确认。

确认内容需要说明：

- 当前状态会被保存。
- 下次打开会继续恢复。
- 退出程序不是暂停作息。

### 11.3 退出不是暂停

退出程序不等于暂停作息。

只有突发事件会暂停作息时间。

下次打开时，根据退出前状态和真实经过时间恢复。

### 11.4 恢复规则

退出时如果处于等待开始确认：

- 下次打开仍然等待该阶段开始确认。
- 关闭期间的时间算作等待延迟。

退出时如果处于阶段进行中：

- 如果还没到预计结束时间，恢复为进行中。
- 如果已经过了预计结束时间，恢复为等待结束确认。
- 关闭期间的时间算作该阶段真实经过时间。

退出时如果处于等待结束确认：

- 下次打开仍然等待结束确认。
- 关闭期间的时间算作当前阶段延长时间。

退出时如果处于突发事件中：

- 下次打开仍然处于突发事件中。
- 关闭期间的时间算作突发事件时间。
- 不算作作息时间。

### 11.5 错过提醒

如果程序在应该提醒时没有运行，重新打开后：

- 只补当前最需要处理的确认。
- 不连续弹出一串历史提醒。
- 不自动越过未确认步骤。

示例：

如果关闭期间跨过了开始提醒和结束提醒，但开始从未确认，则恢复后只提示确认开始。

## 12. 提醒规则

### 12.1 提醒方式

第一版提醒包括：

- 桌面提醒弹窗。
- Windows 系统通知。
- 系统提示音。

核心确认动作必须在程序主界面或提醒弹窗中完成。

系统通知只作为提醒和入口，不提供确认按钮。

### 12.2 周期提醒

未确认期间，程序周期性提醒。

默认提醒间隔：

```text
5 分钟
```

设置页允许修改，例如：

- 1 分钟
- 3 分钟
- 5 分钟
- 10 分钟
- 15 分钟

声音默认每次周期提醒都播放。

声音模式支持：

- 每次周期提醒都响
- 仅首次响
- 静音

### 12.3 提醒窗口行为

提醒窗口：

- 默认置顶。
- 允许关闭。
- 允许最小化。
- 关闭或最小化不改变日程状态。
- 周期提醒到来时，如果窗口已关闭，则重新打开。
- 如果窗口已存在，则唤醒、置顶、闪烁并播放声音。
- 不允许堆叠多个提醒窗口。

### 12.4 稍后提醒

提醒弹窗中的“稍后提醒”：

- 只关闭当前提醒窗口。
- 不改变任何日程状态。
- 不写入历史操作事件。
- 下一个周期提醒时再次提醒。

### 12.5 通知不可用

如果 Windows 系统通知不可用、权限不足或被关闭：

- 桌面提醒弹窗和声音仍然工作。
- 设置页显示“系统通知可能不可用”的状态提示。

## 13. 提醒弹窗按钮

根据状态显示不同按钮。

### 13.1 等待开始确认

按钮：

- 确认开始
- 突发事件
- 稍后提醒

### 13.2 阶段进行中

主界面中应提供：

- 突发事件
- 提前结束

阶段进行中通常不需要提醒弹窗，除非后续设计增加主动查看弹窗。

### 13.3 等待结束确认

按钮：

- 确认结束
- 继续当前阶段
- 突发事件
- 稍后提醒

“继续当前阶段”表示关闭提醒窗口，当前阶段继续进行，并等待下次周期提醒。它不改变日程状态。

## 14. 高影响操作和二次确认

以下操作需要二次确认：

- 退出程序。
- 重置当前作息日。
- 应用最新日程。
- 放弃当前阶段或终止未完成阶段。
- 提前结束。
- 立即进入下一阶段。

以下操作不需要二次确认：

- 确认开始。
- 确认结束。
- 稍后提醒。
- 开始突发事件。
- 结束突发事件。

## 15. 主界面需求

主界面以“今日时间线”为核心。

### 15.1 顶部状态区

顶部显示：

- 当前作息日日期或作息日范围。
- 当前状态。
- 当前阶段名称。
- 下一步需要用户执行的动作。
- 如果正在进行中，显示距离预计结束的倒计时和已进行时间。
- 如果等待确认，显示已等待多久。
- 如果超时等待结束，显示已超时多久。
- 如果突发事件中，显示突发事件已持续多久。

### 15.2 今日时间线

中间主区域是一条可滚动时间线。

每个阶段显示：

- 阶段名称。
- 阶段编号。
- 状态。
- 原计划时间。
- 当前计划时间。
- 实际时间。
- 提前或延后说明。
- 可选备注。

阶段名称允许重复，因此内部必须用顺序编号或唯一 ID 区分。

示例展示：

```text
第 3 段：学习
原计划：10:00-11:30
当前计划：10:17-11:47
实际：10:17-进行中
状态：进行中
```

未开始阶段：

```text
实际：未开始
```

等待开始确认：

```text
实际：等待确认，已等待 7 分钟
```

### 15.3 操作区

主界面需要提供以下操作：

- 当前阶段确认操作。
- 开始突发事件。
- 结束突发事件。
- 提前结束。
- 立即进入下一阶段。
- 重置当前作息日。
- 应用最新日程。
- 打开设置。
- 打开历史。
- 退出程序。

按钮可根据状态启用或禁用。

### 15.4 视觉风格

界面风格：

- 中文界面。
- 安静、清晰、工具型。
- 少动效。
- 优先可读性和可靠操作。

颜色用于辅助区分状态，但不能只靠颜色表达。

建议状态色：

- 进行中：蓝色或绿色。
- 等待确认：橙色。
- 超时或长期未确认：红色。
- 突发事件：紫色或灰色。
- 空档：灰色。

### 15.5 主题

第一版核心保证浅色主题好用。

如果实现成本低，可以支持跟随系统主题，但不作为第一版验收重点。

## 16. 窗口和托盘

### 16.1 主窗口置顶

主界面默认不置顶。

设置中可以开启主界面置顶。

### 16.2 提醒窗口置顶

提醒弹窗默认置顶。

如果主界面和提醒弹窗都置顶：

- 提醒弹窗优先级高于主界面。
- 提醒弹窗应显示在主界面之上。

### 16.3 托盘菜单

第一版托盘菜单保持简单，只包含：

- 打开主界面
- 退出

其他操作暂不放入托盘菜单。

### 16.4 单实例

程序必须是单实例。

如果用户重复启动程序：

- 不创建第二个独立实例。
- 唤起已有主窗口。

## 17. 设置页

第一版设置页只包含必要项：

- 作息 Markdown 文件路径。
- 提醒间隔。
- 声音模式。
- 开机自启动。
- 主窗口是否置顶。
- 应用最新日程按钮。
- 系统通知可用状态提示。

不暴露：

- 数据库存储位置。
- 数据库维护。
- 自动备份。
- 高级统计。
- 主题外观细项。

### 17.1 开机自启动

提供设置项。

默认关闭。

用户可手动开启。

## 18. 历史记录

### 18.1 历史归档单位

历史按作息日查看，而不是按自然日。

如果日程起点是 08:00，则历史页中的一天应覆盖：

```text
当天 08:00 到次日 08:00 前
```

### 18.2 历史内容

历史页需要显示完整时间线，包括：

- 正式阶段。
- 自动空档。
- 提前结束产生的空档。
- 突发事件。
- 手动重置事件。
- 应用最新日程事件。
- 未完成或被终止的阶段。

### 18.3 历史不可编辑

第一版历史记录只读。

不允许：

- 编辑实际开始时间。
- 编辑实际结束时间。
- 删除历史阶段。
- 修改突发事件时间。
- 补录历史。

如果误操作，第一版只能通过重置当前作息日等流程处理，不直接修改历史。

### 18.4 未来导出铺垫

第一版不做导出按钮。

但数据模型应能支持未来导出：

- Markdown
- CSV

因此历史记录中应尽量保存结构化字段，而不是只保存拼接后的文本。

## 19. 数据和持久化

### 19.1 数据位置

程序安装目录只放程序本体。

配置、状态、历史数据库和模板快照保存在 Windows 用户应用数据目录，例如：

```text
%APPDATA%
```

或：

```text
%LOCALAPPDATA%
```

具体路径由实现决定，但必须符合 Windows 用户应用数据目录规范。

### 19.2 需要持久化的数据

至少保存：

- 用户配置。
- 作息 Markdown 文件路径。
- 最近一次成功解析的模板快照。
- 当前作息日状态。
- 当前阶段状态。
- 事件流。
- 历史记录。
- 提醒设置。
- 窗口设置。
- 开机自启动设置。

### 19.3 最近成功模板快照

程序应保存最近一次成功解析并应用的模板快照。

用途：

- Markdown 文件后来写坏时，不影响当前可用日程。
- 程序重启后可以恢复当前作息。
- 数据异常时至少能帮助用户重新开始。

### 19.4 数据损坏

如果数据库或状态文件损坏：

- 不尝试静默修复。
- 启动时提示错误。
- 提供“备份损坏数据并重新初始化”的操作。
- 自动备份功能第一版不做。

### 19.5 概念数据模型

第一版推荐使用“事件流 + 当前状态快照”的方式保存数据。

事件流用于保证历史可追溯，当前状态快照用于快速恢复界面和提醒。

至少需要以下概念实体。

#### 19.5.1 应用配置

保存用户设置。

字段含义至少包括：

- 作息 Markdown 文件路径。
- 提醒间隔分钟数。
- 声音模式。
- 是否开机自启动。
- 主窗口是否置顶。
- 最近一次成功应用的模板 ID。
- 当前活动作息日 ID。

#### 19.5.2 模板快照

每次成功应用 Markdown 模板时，应保存一份解析后的快照。

快照保存的是当时成功应用的结构化结果，而不是只保存文件路径。

字段含义至少包括：

- 模板 ID。
- 原始 Markdown 文件路径。
- 原始 Markdown 文本或文本摘要。
- 解析后的日程起点。
- 解析后的阶段列表。
- 应用时间。
- 是否为当前使用中的模板。

保存模板快照的目的：

- Markdown 文件后续写坏时，不影响当前作息。
- 历史记录能知道当天使用的是哪一版模板。
- 应用最新日程时可以产生新的模板快照。

#### 19.5.3 作息日记录

每个作息日生成一条记录。

字段含义至少包括：

- 作息日 ID。
- 使用的模板 ID。
- 作息日起点实际日期时间。
- 作息日理论结束日期时间。
- 当前状态。
- 当前阶段序号。
- 创建时间。
- 完成时间。
- 是否被手动重置。
- 是否因应用新日程而终止。

作息日按日程起点归档，不按自然日归档。

#### 19.5.4 阶段实例

模板中的正式阶段在某个作息日中会生成阶段实例。

字段含义至少包括：

- 阶段实例 ID。
- 作息日 ID。
- 模板阶段序号。
- 阶段名称。
- 可选备注。
- 原计划开始时间。
- 原计划结束时间。
- 当前计划开始时间。
- 当前计划结束时间。
- 实际开始时间。
- 实际结束时间。
- 状态。
- 是否提前结束。
- 是否被终止。
- 终止原因。

阶段名称允许重复，因此不能用阶段名称作为唯一标识。

#### 19.5.5 时间线片段

为了显示历史和空档，建议保存或可推导时间线片段。

片段类型至少包括：

- 正式阶段。
- 自动空档。
- 提前结束产生的空档。
- 突发事件。
- 手动重置事件。
- 应用最新日程事件。

每个片段至少需要：

- 片段 ID。
- 作息日 ID。
- 类型。
- 名称。
- 开始时间。
- 结束时间。
- 关联阶段 ID。
- 备注。

如果实现上选择不持久化所有片段，也必须能从事件流稳定推导出同样的显示结果。

#### 19.5.6 事件记录

事件记录是历史真实发生的操作。

事件时间必须使用实际发生时刻。

事件类型至少包括：

- 启用模板。
- 应用最新日程。
- 作息日创建。
- 阶段到达开始时间。
- 确认阶段开始。
- 阶段到达结束时间。
- 确认阶段结束。
- 提前结束阶段。
- 立即进入下一阶段。
- 开始突发事件。
- 结束突发事件。
- 手动重置当前作息日。
- 程序退出确认。
- 程序启动恢复。

不是所有事件都必须在界面上展示，但影响时间线和恢复的事件必须保存。

#### 19.5.7 当前状态快照

当前状态快照用于程序启动时快速恢复。

字段含义至少包括：

- 当前作息日 ID。
- 当前状态类型。
- 当前阶段实例 ID。
- 当前突发事件 ID。
- 上一次提醒时间。
- 下一次应提醒时间。
- 当前提醒窗口是否已显示。
- 当前计划开始和结束时间。
- 需要用户处理的动作。

状态快照可以由事件流重建，但第一版建议直接保存，减少启动复杂度。

### 19.6 状态计算原则

实现时应把事件流作为真实来源。

当前计划时间可以缓存，但不能和事件流矛盾。

如果事件流和快照不一致，推荐处理：

- 优先信任事件流。
- 尝试重建当前状态。
- 如果无法重建，提示数据错误，不静默覆盖。

实际时间规则：

- 确认开始时间等于点击确认开始的时刻。
- 确认结束时间等于点击确认结束的时刻。
- 提前结束时间等于确认提前结束的时刻。
- 突发事件开始时间等于点击突发事件的时刻。
- 突发事件结束时间等于点击结束突发事件的时刻。
- 第一版不允许用户手动改这些时间。

### 19.7 调度和计时原则

程序需要区分两类计时：

- 界面刷新计时。
- 状态和提醒调度。

界面刷新可以较频繁，例如每秒刷新倒计时、正计时和突发事件计时器。

状态和提醒调度必须基于系统真实时间计算，不能只依赖前端倒计时。

程序恢复、睡眠唤醒或重新打开时，应立即做一次状态校准：

- 读取当前状态快照。
- 读取相关事件流。
- 获取当前真实时间。
- 判断是否已经到达开始提醒、结束提醒或周期提醒时间。
- 只触发当前状态需要的提醒，不补一串旧提醒。

### 19.8 提醒去重原则

同一时刻只能存在一个活动提醒窗口。

提醒去重建议按“当前待处理动作”判断。

例如：

- 当前待处理动作是确认第 3 阶段开始。
- 周期提醒触发多次，也只能复用同一个提醒窗口。
- 如果用户关闭窗口，下次周期提醒重新打开。
- 如果用户已确认开始，旧的确认开始提醒必须失效。

### 19.9 Markdown 解析输出要求

Markdown 解析成功后，输出的结构化模板至少应包含：

- 日程起点。
- 阶段顺序号。
- 阶段名称。
- 阶段备注。
- 原始开始时间。
- 原始结束时间。
- 是否跨午夜。
- 阶段持续分钟数。

解析器应在应用前完成全部校验。

不能一边应用一边发现错误。

## 20. Markdown 解析错误处理

如果 Markdown 文件解析失败：

- 不应用坏文件。
- 不破坏当前正在运行的作息日。
- 主界面或设置页显示错误。
- 错误应尽量指出行号和原因。
- 用户修正后，需要再次手动应用最新日程。

常见错误包括：

- 时间格式错误。
- 阶段缺少名称。
- 阶段缺少开始或结束时间。
- 阶段重叠。
- 阶段顺序不合理。
- 没有任何有效阶段。
- 日程起点格式错误。

## 21. 状态显示规则

每个阶段在主界面和历史中至少显示三组时间。

### 21.1 原计划

来自 Markdown 模板。

示例：

```text
原计划：10:00-11:30
```

### 21.2 当前计划

根据事件流计算。

示例：

```text
当前计划：10:17-11:47
```

如果当前计划早于原计划，需要显示提前：

```text
当前计划：09:52-11:22，提前 8 分钟
```

如果当前计划晚于原计划，需要显示延后：

```text
当前计划：10:17-11:47，延后 17 分钟
```

### 21.3 实际

根据用户操作记录。

可能显示：

- 未开始
- 等待确认，已等待多久
- 进行中
- 实际开始到实际结束
- 实际开始到进行中
- 已提前结束
- 已终止
- 未完成

## 22. 突发事件详细需求

### 22.1 开始突发事件

用户点击突发事件按钮后：

- 立即进入突发事件状态。
- 不需要二次确认。
- 记录开始时间。
- 显示突发事件持续计时器。

### 22.2 结束突发事件

用户点击结束突发事件后：

- 不需要二次确认。
- 记录结束时间。
- 弹出或显示可选备注输入。
- 用户可以不填。
- 不填时记录名称为“突发事件”。

### 22.3 突发事件计时器

突发事件期间，主界面必须明显显示：

```text
突发事件进行中，已持续 HH:MM:SS
```

并提供结束突发事件按钮。

## 23. 阶段进行中显示

阶段进行中时，主视觉以倒计时为主，同时显示正计时。

示例：

```text
学习进行中
距离预计结束：00:42:13
已进行：00:47:47
```

如果超过预计结束还没确认结束，切换为等待结束确认：

```text
学习等待结束确认
已超时：00:08:12
```

## 24. 作息日跨越和异常残留

如果到了下一作息日起点，但上一作息日还未完成：

- 不自动重置。
- 不自动进入新作息日。
- 保留当前状态。
- 显示提示：可能忘记确认。
- 用户可选择手动重置当前作息日。

这种情况多数是用户忘记确认导致，需要兜底但不复杂化自动逻辑。

## 25. 验收场景

以下场景应作为第一版手动测试或自动测试依据。

### 25.1 正常执行

模板：

```text
10:00-11:30 学习
11:30-13:00 吃饭
13:00-14:30 学习
```

预期：

- 10:00 提醒确认开始学习。
- 确认后进入学习进行中。
- 11:30 提醒确认结束学习。
- 确认后进入吃饭等待开始确认。
- 每个阶段都需要确认开始和结束。

### 25.2 开始延迟

原计划 10:00 开始学习。

用户 10:17 确认开始。

预期：

- 学习实际开始为 10:17。
- 学习当前计划结束为 11:47。
- 后续阶段延后 17 分钟。
- 原计划仍显示 10:00-11:30。

### 25.3 结束延迟

学习当前计划 10:17-11:47。

用户 12:05 确认结束。

预期：

- 学习实际结束为 12:05。
- 学习延长 18 分钟。
- 后续阶段额外延后 18 分钟。

### 25.4 提前结束

学习 10:00-11:30。

用户 11:00 提前结束。

预期：

- 需要二次确认。
- 学习实际结束为 11:00。
- 11:00-11:30 形成空档。
- 下一阶段默认仍按当前计划开始。

### 25.5 立即进入下一阶段

学习 10:00-11:30。

用户 11:00 提前结束并选择立即进入下一阶段。

预期：

- 立即进入下一阶段需要二次确认。
- 下一阶段实际开始为确认时刻。
- 后续当前计划可能早于原计划。
- 界面显示提前量。

### 25.6 突发事件发生在阶段中

学习 10:00-11:30。

10:40 开始突发事件，11:10 结束。

预期：

- 突发事件期间学习暂停。
- 学习结束顺延到 12:00。
- 突发事件单独记录。

### 25.7 突发事件发生在等待确认中

10:00 等待开始学习。

10:05 开始突发事件，10:35 结束，10:40 确认开始。

预期：

- 突发事件 30 分钟不算等待延迟。
- 开始延迟为 10 分钟。

### 25.8 突发事件发生在空档中且不越界

空档 09:00-10:00。

09:20 开始突发事件，09:40 结束。

预期：

- 后续 10:00 阶段不顺延。
- 历史显示空档、突发事件、空档三段。

### 25.9 突发事件跨过下一阶段开始

空档 09:00-10:00，学习 10:00-11:30。

09:50 开始突发事件，10:20 结束。

预期：

- 学习当前计划开始为 10:20。
- 恢复后等待学习开始确认。
- 10:00-10:20 算突发事件顺延，不算开始延迟。

### 25.10 程序退出后恢复

10:00 等待开始确认，用户退出程序，10:30 打开。

预期：

- 程序仍等待该阶段开始确认。
- 显示已等待 30 分钟。
- 不自动跳过阶段。

### 25.11 进行中时退出

10:00 确认开始学习，计划 11:30 结束。

10:30 退出程序，12:00 打开。

预期：

- 程序恢复为等待结束确认。
- 显示已超时 30 分钟。
- 不自动结束学习。

### 25.12 突发事件中退出

10:30 开始突发事件。

10:40 退出程序，11:10 打开。

预期：

- 仍处于突发事件中。
- 突发事件持续时间包含关闭期间。
- 作息时间不流动。

### 25.13 Markdown 修改不自动影响当前日程

当前作息日正在运行。

用户修改 Markdown 文件。

预期：

- 当前作息不变。
- 设置页或主界面可以提示有新文件内容。
- 用户必须手动应用最新日程。

### 25.14 Markdown 解析失败

用户应用一个有重叠阶段的 Markdown 文件。

预期：

- 显示解析错误和行号。
- 不应用坏模板。
- 当前日程不受影响。

### 25.15 跨午夜阶段

模板：

```text
23:30-00:30 学习
```

预期：

- 该阶段从当前作息日 23:30 到次日 00:30。
- 历史归档到当前作息日，而不是被自然日切断。

## 26. 后续可扩展方向

以下功能不是第一版，但设计时应尽量不堵死：

- 多模板。
- 工作日和周末规则。
- 按具体日期覆盖日程。
- 历史导出 Markdown。
- 历史导出 CSV。
- 统计图表。
- 自定义提醒音频。
- 全局快捷键。
- 免打扰模式。
- 自动备份。
- 便携版。
- 时间模拟或测试模式。
- 手动补录和历史修正。
- 更复杂的阶段字段，例如标签、优先级、地点、任务清单。

## 27. 第一版验收标准

第一版可以认为完成，当且仅当：

- 用户可以选择 Markdown 作息文件并启用。
- 程序可以生成今日作息时间线。
- 每个正式阶段都要求确认开始和确认结束。
- 未确认时不会自动进入下一阶段。
- 延迟、提前、突发事件都能正确影响当前计划。
- 原计划、当前计划、实际时间能同时显示。
- 突发事件能暂停作息时间，并在恢复后正确顺延。
- 自动空档能显示并记录。
- 提醒弹窗、系统通知和声音提醒可用。
- 周期提醒默认 5 分钟。
- 程序关闭窗口后隐藏到托盘。
- 真正退出前会确认。
- 程序退出、重启、睡眠恢复后能恢复正确状态。
- 历史记录按作息日查看。
- Markdown 解析错误不会破坏当前日程。
- 数据完全保存在本地。
- 程序是单实例。
